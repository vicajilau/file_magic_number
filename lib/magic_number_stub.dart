import 'package:file_magic_number/magic_number_type.dart';

class MagicNumber {
  static Future<MagicNumberType> detectFileType(dynamic file) async {
    throw UnimplementedError('Platform not supported');
  }
}
